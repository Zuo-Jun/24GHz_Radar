# 下水道雷达外设模块使用说明

## 目录

- [1. 系统概述](#1-系统概述)
- [2. ADF4153A PLL锁相环](#2-adf4153a-pll锁相环)
- [3. BGT24MTR11 24GHz雷达MMIC](#3-bgt24mtr11-24ghz雷达mmic)
- [4. PGA113 可编程增益放大器](#4-pga113-可编程增益放大器)
- [5. SP3485E RS-485收发器](#5-sp3485e-rs-485收发器)
- [6. ADXL345 三轴加速度计](#6-adxl345-三轴加速度计)
- [7. 雷达信号处理模块](#7-雷达信号处理模块)
- [8. Radar_protocol 通信协议层](#8-radar_protocol-通信协议层)
- [9. 系统状态变量管理](#9-系统状态变量管理)
- [10. 典型应用示例](#10-典型应用示例)

---

## 1. 系统概述

本项目是基于STM32F405RGT6的24GHz FMCW雷达系统，用于下水道水位和水速测量。

### 硬件架构

```
                    ┌─────────────────────────────────────────┐
                    │              STM32F405RGT6              │
                    │                                         │
    25MHz TCXO ───►│  ADF4153A ◄──────► BGT24MTR11          │
                    │    (PLL)              (雷达前端)         │
                    │                          │              │
                    │                      I/Q信号            │
                    │                          ▼              │
                    │                     PGA113 ──► ADC      │
                    │                    (放大器)    (双通道)   │
                    │                                         │
                    │     RS-485 ◄──────► 上位机通信          │
                    │    (SP3485E)                            │
                    │                                         │
                    │     ADXL345 ◄─────► 姿态检测            │
                    │    (加速度计)                            │
                    └─────────────────────────────────────────┘
```

### 文件结构

| 文件                   | 功能描述                        |
| ---------------------- | ------------------------------- |
| `ADF4153A.h/c`       | PLL锁相环驱动（软件SPI）        |
| `BGT24MTR11.h/c`     | 24GHz雷达MMIC驱动（软件SPI）    |
| `PGA113.h/c`         | 可编程增益放大器驱动（软件SPI） |
| `SP3485E.h/c`        | RS-485收发器驱动（UART）        |
| `ADXL345.h/c`        | 三轴加速度计驱动（I2C）         |
| `Radar_config.h`     | 雷达系统参数配置                |
| `Radar_chirp.h/c`    | Chirp扫频与ADC采集              |
| `Radar_frame.h/c`    | 帧处理与DSP算法                 |
| `Radar_protocol.h/c` | RS-485通信协议层（CRC+SEQ+ACK） |
| `main.c`             | 主程序入口                      |

---

## 2. ADF4153A PLL锁相环

### 2.1 模块功能

ADF4153A是分数N锁相环，用于产生可控的本振信号，控制BGT24MTR11的发射频率。

- 参考输入：25MHz TCXO
- RF输入：来自BGT24MTR11的÷16预分频输出（约1.5GHz）
- 接口：软件模拟SPI（24位数据传输）

### 2.2 引脚定义

| 宏定义               | GPIO | 功能         |
| -------------------- | ---- | ------------ |
| `ADF_LE_Pin`       | PB13 | 锁存使能     |
| `SoftSPI_DATA_Pin` | PB8  | MOSI数据线   |
| `SoftSPI_CLK_Pin`  | PB9  | SPI时钟      |
| `ADF_MUXOUT_Pin`   | PB0  | 锁定检测输出 |

### 2.3 数据结构

```c
/* MUXOUT模式枚举 */
typedef enum {
    MUXOUT_DLOCK_DIGITAL  = 1,  /* 数字锁定检测（推荐）*/
    MUXOUT_THREE_STATE    = 0,  /* 高阻态 */
    MUXOUT_N_DIV_OUT      = 2,  /* N分频器输出 */
    MUXOUT_LOGIC_HIGH     = 3,  /* 恒高 */
    // ... 其他模式
} ADF_MuxoutMode_t;

/* 噪声/毛刺模式枚举 */
typedef enum {
    NOISE_SPUR_LOW_SPUR       = 0x00,  /* 低毛刺模式 */
    NOISE_SPUR_LOW_NOISE_SPUR = 0x1C,  /* 平衡模式 */
    NOISE_SPUR_LOWEST_NOISE   = 0x1F,  /* 最低噪声（推荐）*/
} ADF_NoiseSpur_t;

/* 配置结构体 */
typedef struct {
    uint8_t  fastlock_en;      /* 快速锁定使能 */
    uint32_t INT;              /* 整数分频值 31~511 */
    uint32_t FRAC;             /* 小数分子 0~MOD-1 */

    uint8_t resync_en;         /* 相位重同步使能 */
    ADF_MuxoutMode_t muxout;   /* MUXOUT功能 */
    uint8_t  prescaler;        /* 预分频器：0=4/5, 1=8/9 */
    uint8_t  R;                /* 参考分频 1~15 */
    uint32_t MOD;              /* 小数分母 2~4095 */

    uint8_t  resync;           /* 相位重同步值 */
    uint8_t  refin_doubler;    /* 参考倍频 */
    uint8_t  cp_current;       /* 电荷泵电流 0~15 */
    uint8_t  pd_polarity;      /* 鉴相极性 */
    uint8_t  ldp;              /* 锁定检测精度 */
    ADF_NoiseSpur_t noise_spur;/* 噪声模式 */
} ADF4153A_Config_t;
```

### 2.4 API函数

| 函数                        | 功能           | 参数                | 返回值 |
| --------------------------- | -------------- | ------------------- | ------ |
| `ADF4153A_Init()`         | 初始化PLL      | 配置结构体指针      | void   |
| `ADF4153A_SetFrequency()` | 设置输出频率   | INT, FRAC, MOD      | void   |
| `ADF4153A_WaitLock()`     | 等待锁定       | 超时时间(ms)        | bool   |
| `ADF4153A_PowerDown()`    | 软件掉电       | 使能标志            | void   |
| `ADF4153A_CalcFpfd()`     | 计算PFD频率    | 参考频率, 倍频器, R | double |
| `ADF4153A_CalcRFout()`    | 计算RF输出频率 | 各分频参数          | double |

### 2.5 使用示例

```c
#include "ADF4153A.h"

/* PLL初始化配置（起始频率24GHz）*/
ADF4153A_Config_t pll_cfg = {
    .fastlock_en    = 0,
    .INT            = 60,           /* 1500MHz/25MHz = 60 */
    .FRAC           = 0,
    .resync_en      = 0,
    .muxout         = MUXOUT_DLOCK_DIGITAL,
    .prescaler      = 0,            /* 4/5模式 */
    .R              = 1,
    .MOD            = 125,
    .resync         = 0,
    .refin_doubler  = 0,
    .cp_current     = 7,
    .pd_polarity    = 1,
    .ldp            = 0,
    .noise_spur     = NOISE_SPUR_LOWEST_NOISE,
};

/* 初始化并等待锁定 */
ADF4153A_Init(&pll_cfg);
if (ADF4153A_WaitLock(500)) {
    printf("PLL locked!\n");
} else {
    printf("PLL lock failed!\n");
}

/* 动态修改频率（扫频时使用）*/
ADF4153A_SetFrequency(61, 62, 125);  /* INT=61, FRAC=62, MOD=125 */
```

---

## 3. BGT24MTR11 24GHz雷达MMIC

### 3.1 模块功能

BGT24MTR11是24GHz雷达收发前端芯片，集成VCO、PA、LNA和分频器。

- 发射功率：最大+11dBm
- 内置÷16预分频器（输出到ADF4153A）
- 支持温度传感器输出

### 3.2 引脚定义

| 宏定义               | GPIO | 功能                  |
| -------------------- | ---- | --------------------- |
| `BGT_CS_Pin`       | PB12 | SPI片选               |
| `SoftSPI_DATA_Pin` | PB8  | MOSI数据线            |
| `SoftSPI_CLK_Pin`  | PB9  | SPI时钟               |
| `BGT_ANA_Pin`      | PA4  | 模拟输出（温度/功率） |

### 3.3 数据结构

```c
/* AMUX输出选择枚举 */
typedef enum {
    BGT_AMUX_VOUT_TX  = 0x00,  /* TX功率传感器 */
    BGT_AMUX_VREF_TX  = 0x01,  /* TX参考电压 */
    BGT_AMUX_VOUT_LO  = 0x02,  /* LO功率传感器 */
    BGT_AMUX_VREF_LO  = 0x03,  /* LO参考电压 */
    BGT_AMUX_VTEMP    = 0x04,  /* 温度传感器 */
} BGT_AmuxSel_t;

/* 配置结构体 */
typedef struct {
    uint8_t  gs;              /* LNA增益降低：0=正常, 1=降低5dB */
    uint8_t  tx_disable;      /* TX禁用：0=允许发射, 1=禁止 */
    uint8_t  tx_pa_level;     /* PA功率级别 0~7（7=最大+11dBm）*/
    uint8_t  tx_buf_high_pwr; /* TX Buffer高功率 */
    uint8_t  lo_buf_high_pwr; /* LO Buffer高功率 */
    uint8_t  dis_div64k;      /* 禁用÷65536分频 */
    uint8_t  dis_div16;       /* 禁用÷16分频（必须为0！）*/
    BGT_AmuxSel_t amux;       /* ANA引脚输出选择 */
} BGT24MTR11_Config_t;
```

### 3.4 API函数

| 函数                     | 功能        | 参数           | 返回值 |
| ------------------------ | ----------- | -------------- | ------ |
| `BGT24MTR11_Init()`    | 初始化芯片  | 配置结构体指针 | void   |
| `BGT24MTR11_SetAmux()` | 切换ANA输出 | AMUX选择       | void   |

### 3.5 使用示例

```c
#include "BGT24MTR11.h"

/* 雷达前端初始化配置 */
BGT24MTR11_Config_t bgt_cfg = {
    .gs              = 0,              /* 正常LNA增益 */
    .tx_disable      = 0,              /* 允许发射 */
    .tx_pa_level     = 7,              /* PA最大功率 */
    .tx_buf_high_pwr = 1,              /* TX Buffer高功率 */
    .lo_buf_high_pwr = 1,              /* LO Buffer高功率 */
    .dis_div16       = 0,              /* 必须为0！ */
    .dis_div64k      = 1,              /* 禁用Q2输出 */
    .amux            = BGT_AMUX_VTEMP, /* 温度输出 */
};

BGT24MTR11_Init(&bgt_cfg);

/* 动态切换读取温度或功率 */
BGT24MTR11_SetAmux(BGT_AMUX_VTEMP);    /* 切换到温度输出 */
BGT24MTR11_SetAmux(BGT_AMUX_VOUT_TX);  /* 切换到TX功率输出 */
```

---

## 4. PGA113 可编程增益放大器

### 4.1 模块功能

PGA113是零漂移可编程增益放大器，用于放大中频信号至ADC最佳采样范围。

- 增益范围：×1 ~ ×200（8档可选）
- 两片PGA113共用同一SPI总线
- 支持软件关断模式（<4μA）

### 4.2 引脚定义

| 宏定义                   | GPIO | 功能           |
| ------------------------ | ---- | -------------- |
| `PGA113_CS_GPIO_PIN`   | PB11 | SPI片选        |
| `PGA113_SCLK_GPIO_PIN` | PB9  | SPI时钟        |
| `PGA113_DIO_GPIO_PIN`  | PB8  | 数据线（只写） |

### 4.3 数据结构

```c
/* 增益枚举 */
typedef enum {
    PGA113_GAIN_1   = 0x00U,
    PGA113_GAIN_2   = 0x01U,
    PGA113_GAIN_5   = 0x02U,
    PGA113_GAIN_10  = 0x03U,
    PGA113_GAIN_20  = 0x04U,
    PGA113_GAIN_50  = 0x05U,
    PGA113_GAIN_100 = 0x06U,
    PGA113_GAIN_200 = 0x07U,
} PGA113_Gain_t;

/* 输入通道枚举 */
typedef enum {
    PGA113_CH_VCAL_CH0 = 0x00U,  /* 校准基准输入 */
    PGA113_CH_CH1      = 0x01U,  /* 信号输入（中频）*/
    PGA113_CH_CAL1     = 0x0CU,  /* 内部校准：GND */
    PGA113_CH_CAL2     = 0x0DU,  /* 内部校准：0.9×VCAL */
    PGA113_CH_CAL3     = 0x0EU,  /* 内部校准：0.1×VCAL */
    PGA113_CH_CAL4     = 0x0FU,  /* 内部校准：VREF */
} PGA113_Channel_t;
```

### 4.4 API函数

| 函数                        | 功能           | 参数       | 返回值 |
| --------------------------- | -------------- | ---------- | ------ |
| `PGA113_Init()`           | 初始化         | 无         | void   |
| `PGA113_SetGainChannel()` | 设置增益和通道 | 增益, 通道 | void   |
| `PGA113_Shutdown()`       | 进入关断模式   | 无         | void   |
| `PGA113_WakeUp()`         | 退出关断模式   | 无         | void   |

### 4.5 使用示例

```c
#include "PGA113.h"

/* 初始化（默认增益×1，通道CH1）*/
PGA113_Init();

/* 设置增益×20，使用CH1通道 */
PGA113_SetGainChannel(PGA113_GAIN_20, PGA113_CH_CH1);

/* 进入低功耗模式 */
PGA113_Shutdown();

/* 唤醒 */
PGA113_WakeUp();
```

---

## 5. SP3485E RS-485收发器

### 5.1 模块功能

SP3485E是3.3V半双工RS-485收发器，用于与上位机通信。

- 最高速率：12Mbps
- 内置环形接收缓冲区（256字节）
- 自动方向切换（发送完成自动切回接收）

### 5.2 引脚定义

| 宏定义                 | GPIO | 功能            |
| ---------------------- | ---- | --------------- |
| `SP3485E_Switch_Pin` | PC4  | 方向控制（DIR） |
| UART4                  | -    | 数据收发        |

### 5.3 数据结构

```c
/* 状态枚举 */
typedef enum {
    RS485_OK        = 0,
    RS485_TIMEOUT   = 1,
    RS485_OVERFLOW  = 2,
    RS485_ERROR     = 3,
} RS485_Status_t;

/* 句柄结构体 */
typedef struct {
    uint8_t  rx_buf[RS485_RX_BUF_SIZE]; /* 接收环形缓冲区 */
    uint16_t rx_head;                   /* 写指针 */
    uint16_t rx_tail;                   /* 读指针 */
    volatile bool tx_busy;              /* 发送忙标志 */
    volatile bool tx_done;              /* 发送完成标志 */
} RS485_Handle_t;

extern RS485_Handle_t g_rs485;  /* 全局句柄 */
```

### 5.4 API函数

| 函数                  | 功能           | 参数             | 返回值         |
| --------------------- | -------------- | ---------------- | -------------- |
| `RS485_Init()`      | 初始化         | 无               | void           |
| `RS485_Send()`      | 发送数据       | 数据指针, 长度   | RS485_Status_t |
| `RS485_SendStr()`   | 发送字符串     | 字符串指针       | RS485_Status_t |
| `RS485_Available()` | 查询可读字节数 | 无               | uint16_t       |
| `RS485_Read()`      | 读取数据       | 缓冲区, 最大长度 | uint16_t       |
| `RS485_ReadByte()`  | 读取单字节     | 无               | int16_t        |
| `RS485_FlushRx()`   | 清空接收缓冲   | 无               | void           |

### 5.5 内部 inline 函数

以下函数声明为 `static inline`，仅限文件内部使用，实现环形接收缓冲区的底层操作。

| 函数             | 功能                                 | 参数       | 返回值               |
| ---------------- | ------------------------------------ | ---------- | -------------------- |
| `rbuf_next()`  | 环形指针推进（位运算替代取模）       | 当前索引   | uint16_t             |
| `rbuf_empty()` | 判断缓冲区是否为空                   | 无         | bool                 |
| `rbuf_full()`  | 判断缓冲区是否为满（牺牲一空间判满） | 无         | bool                 |
| `rbuf_push()`  | 向缓冲区写入一字节（满则丢弃）       | 待写入字节 | void                 |
| `rbuf_pop()`   | 从缓冲区弹出一字节                   | 无         | int16_t（-1 表示空） |

### 5.6 使用示例

```c
#include "SP3485E.h"

/* 初始化（在UART初始化后调用）*/
RS485_Init();

/* ==========发送功能测试===========*/
/* 发送字符串 */
RS485_SendStr("$RADAR,BOOT:OK\r\n");

/* 发送二进制数据 */
uint8_t data[] = {0xAA, 0xBB, 0xCC, 0xDD, 0xEE};
RS485_Send(data, sizeof(data));

/* ===========接收原样返回测试======*/
 uint16_t avail = RS485_Available();
 if (avail > 0) {
	/* 等一会儿，看还有没有后续字节 */
        uint32_t t0 = HAL_GetTick();
        uint16_t prev = avail;
        while ((HAL_GetTick() - t0) < 20) {  /* 等 20ms 总线静默 */
        	uint16_t now = RS485_Available();
                if (now != prev) {
                    prev = now;
                    t0 = HAL_GetTick();  /* 有新数据，重置定时器 */
                }
            }
	/* 20ms 内没新数据，认为一次收完了 */
        avail = RS485_Available();
        uint8_t buf[64];
        uint16_t len = RS485_Read(buf, avail > sizeof(buf) ? sizeof(buf) : avail);
        RS485_Send(buf, len);
        }
```

---

## 6. ADXL345 三轴加速度计

### 6.1 模块功能

ADXL345是低功耗三轴MEMS加速度计，用于检测系统振动或倾斜角度。

- 量程：±2g/±4g/±8g/±16g
- 分辨率：10位/13位（全分辨率）
- 接口：I2C（400kHz）
- 内置FIFO和多种中断功能

### 6.2 引脚定义

| 引脚 | 连接       | 说明                           |
| ---- | ---------- | ------------------------------ |
| SDA  | PC1 (I2C1) | I2C数据线                      |
| SCL  | PC0 (I2C1) | I2C时钟线                      |
| SDO  | GND/3.3V   | 地址选择（GND=0x53, VCC=0x1D） |
| CS   | 3.3V       | I2C模式必须拉高                |

### 6.3 数据结构

```c
/* 数据速率枚举 */
typedef enum {
    ADXL345_RATE_100   = 0x0AU,   /* 100 Hz（推荐）*/
    ADXL345_RATE_200   = 0x0BU,   /* 200 Hz */
    ADXL345_RATE_400   = 0x0CU,   /* 400 Hz */
    // ... 其他速率
} ADXL345_DataRate_t;

/* 量程枚举 */
typedef enum {
    ADXL345_RANGE_2G   = 0x00U,   /* ±2g */
    ADXL345_RANGE_4G   = 0x01U,   /* ±4g */
    ADXL345_RANGE_8G   = 0x02U,   /* ±8g */
    ADXL345_RANGE_16G  = 0x03U,   /* ±16g */
} ADXL345_Range_t;

/* 加速度数据结构体 */
typedef struct {
    int16_t x;    /* X轴原始值 */
    int16_t y;    /* Y轴原始值 */
    int16_t z;    /* Z轴原始值 */
} ADXL345_AccelData_t;

/* 配置结构体 */
typedef struct {
    ADXL345_DataRate_t  data_rate;      /* 数据速率 */
    ADXL345_Range_t     range;          /* 量程 */
    bool                full_res;       /* 全分辨率模式 */
    bool                low_power;      /* 低功耗模式 */
    ADXL345_FifoMode_t  fifo_mode;      /* FIFO模式 */
    uint8_t             fifo_samples;   /* FIFO采样数 */
    int8_t              offset_x;       /* X轴偏移 */
    int8_t              offset_y;       /* Y轴偏移 */
    int8_t              offset_z;       /* Z轴偏移 */
    bool                addr_high;      /* I2C地址选择 */
} ADXL345_Config_t;
```

### 6.4 API函数

| 函数                           | 功能             | 参数                   | 返回值  |
| ------------------------------ | ---------------- | ---------------------- | ------- |
| `ADXL345_Init()`             | 初始化           | 配置结构体指针         | bool    |
| `ADXL345_GetDeviceID()`      | 读取设备ID       | 无                     | uint8_t |
| `ADXL345_ReadAccel()`        | 读取三轴加速度   | 数据结构体指针         | bool    |
| `ADXL345_RawToG()`           | 原始值转g值      | 原始值, 量程, 全分辨率 | float   |
| `ADXL345_SetDataRate()`      | 设置数据速率     | 速率, 低功耗标志       | void    |
| `ADXL345_SetRange()`         | 设置量程         | 量程, 全分辨率标志     | void    |
| `ADXL345_SetOffset()`        | 设置偏移校准     | x, y, z                | void    |
| `ADXL345_ConfigActivity()`   | 配置活动检测     | 阈值, 耦合方式, 轴使能 | void    |
| `ADXL345_ConfigFreeFall()`   | 配置自由落体检测 | 阈值, 时间             | void    |
| `ADXL345_EnableInterrupt()`  | 使能中断         | 中断类型, 映射引脚     | void    |
| `ADXL345_StartMeasurement()` | 开始测量         | 无                     | void    |
| `ADXL345_StopMeasurement()`  | 停止测量         | 无                     | void    |
| `ADXL345_SoftReset()`        | 软件复位         | 无                     | void    |

### 6.5 使用示例

```c
#include "ADXL345.h"

/* 初始化配置 */
ADXL345_Config_t cfg = {
    .data_rate    = ADXL345_RATE_100,   /* 100Hz */
    .range        = ADXL345_RANGE_2G,   /* ±2g */
    .full_res     = true,               /* 全分辨率模式 */
    .low_power    = false,
    .fifo_mode    = ADXL345_FIFO_BYPASS,
    .fifo_samples = 0,
    .offset_x = 0, .offset_y = 0, .offset_z = 0,
    .addr_high    = false,              /* 地址0x53 */
};

/* 初始化 */
if (ADXL345_Init(&cfg)) {
    printf("ADXL345 initialized, ID=0x%02X\n", ADXL345_GetDeviceID());
}

/* 读取加速度 */
ADXL345_AccelData_t accel;
if (ADXL345_ReadAccel(&accel)) {
    /* 转换为g值 */
    float x_g = ADXL345_RawToG(accel.x, ADXL345_RANGE_2G, true);
    float y_g = ADXL345_RawToG(accel.y, ADXL345_RANGE_2G, true);
    float z_g = ADXL345_RawToG(accel.z, ADXL345_RANGE_2G, true);
    printf("X=%.3fg, Y=%.3fg, Z=%.3fg\n", x_g, y_g, z_g);
}

/* 配置自由落体检测 */
ADXL345_ConfigFreeFall(7, 30);  /* 阈值~440mg, 时间150ms */
ADXL345_EnableInterrupt(ADXL345_INT_FREEFALL, true);  /* 映射到INT1 */

/* 停止测量（省电）*/
ADXL345_StopMeasurement();

/* 重新开始测量 */
ADXL345_StartMeasurement();
```

---

## 7. 雷达信号处理模块

### 7.1 模块功能

雷达信号处理模块包含三个层次：

| 模块      | 文件               | 功能                  |
| --------- | ------------------ | --------------------- |
| 配置参数  | `Radar config.h` | 系统参数定义          |
| Chirp驱动 | `Radar chirp.c`  | 扫频控制与数据采集    |
| 帧处理    | `radar_frame.c`  | FFT、CFAR、物理量计算 |

### 7.2 系统参数（Radar config.h）

```c
/* 雷达频率参数 */
#define RADAR_FC_HZ         24125000000.0   /* 中心频率 24.125GHz */
#define RADAR_F_START_HZ    24000000000.0   /* 扫频起始 24.000GHz */
#define RADAR_F_STOP_HZ     24250000000.0   /* 扫频终止 24.250GHz */
#define RADAR_BW_HZ         250000000.0     /* 带宽 250MHz */

/* 时序参数 */
#define RADAR_TC_S          0.001           /* Chirp周期 1ms */
#define CHIRP_STEPS         79              /* 每次chirp步数 */
#define CHIRP_STEP_US       13              /* 每步间隔 13μs */

/* 帧结构 */
#define N_CHIRPS_WATER_V    16              /* 水速测量三角波对数 */
#define N_CHIRPS_CAR        4               /* 车速测量三角波对数 */

/* ADC参数 */
#define ADC_FS_HZ           200000          /* 采样率 200ksps */

/* 物理量计算 */
#define RADAR_DRANGE        0.60            /* 距离分辨率 0.6m */
#define RADAR_WAVELENGTH    0.01244         /* 波长 12.44mm */
```

### 7.3 Chirp驱动（Radar chirp.c）

```c
/* 数据结构 */
typedef struct {
    int16_t i;   /* I路数据（去直流后）*/
    int16_t q;   /* Q路数据（去直流后）*/
} IQ_Sample_t;

typedef enum {
    CHIRP_UP   = 0,   /* 上扫 */
    CHIRP_DOWN = 1,   /* 下扫 */
} ChirpDir_t;

typedef struct {
    ChirpDir_t  dir;                     /* 扫频方向 */
    uint8_t     step;                    /* 当前步数 */
    bool        done;                    /* 完成标志 */
    IQ_Sample_t iq_buf[CHIRP_STEPS];     /* 采样缓冲区 */
} ChirpState_t;

extern ChirpState_t g_chirp;  /* 全局状态 */
```

#### API函数

| 函数                   | 功能         | 说明                    |
| ---------------------- | ------------ | ----------------------- |
| `Chirp_Precompute()` | 预计算频率表 | Init时调用一次          |
| `Chirp_Start()`      | 启动扫频     | 设置方向、启动ADC和TIM2 |
| `Chirp_WaitDone()`   | 等待完成     | 阻塞等待                |
| `Chirp_StepISR()`    | 步进中断处理 | 定时器ISR中调用         |

#### 内部 static 变量与函数

| 名称                             | 类型                             | 作用                                                                                 |
| -------------------------------- | -------------------------------- | ------------------------------------------------------------------------------------ |
| `s_INT_up[]` / `s_FRAC_up[]` | `static uint32_t[CHIRP_STEPS]` | 上扫频率查找表，预计算后供 ISR 直接查表写入 ADF                                      |
| `s_INT_dn[]` / `s_FRAC_dn[]` | `static uint32_t[CHIRP_STEPS]` | 下扫频率查找表                                                                       |
| `s_iq_raw`                     | `static volatile uint32_t`     | DMA 从 ADC→CDR 搬运的原始 32 位值：`[31:16]=ADC2(Q)`，`[15:0]=ADC1(I)`          |
| `s_dma_done`                   | `static volatile bool`         | DMA 转换完成标志，由 `HAL_ADC_ConvCpltCallback` 置位，`Chirp_StepISR` 消费后清零 |

| 函数                                | 功能                           | 说明                                                  |
| ----------------------------------- | ------------------------------ | ----------------------------------------------------- |
| `FreqToND(vco_hz, &INT, &FRAC)`   | VCO 目标频率 → ADF N 分频参数 | 内部频率换算：`vco_hz/16 → Fpfd × (INT+FRAC/MOD)` |
| `HAL_ADC_ConvCpltCallback()`      | ADC DMA 转换完成回调           | 置位 `s_dma_done`，通知 ISR 本步数据已就绪          |
| `HAL_TIM_PeriodElapsedCallback()` | TIM2 定时器中断回调            | 转发调用 `Chirp_StepISR()`                          |

**Chirp_StepISR 执行流程：**

```
1. DMA 已就绪？→ 读取 s_iq_raw，I=raw[15:0]-2048，Q=raw[31:16]-2048，去直流
2. 还有下一步？→ 查表写下一跳 ADF 频率，重启 DMA，step++
3. 已是最后一步？→ 停止 DMA，停止 TIM2，done=true
```

#### 使用示例

```c
/* 初始化时预计算 */
Chirp_Precompute();

/* 执行一次上扫 */
Chirp_Start(CHIRP_UP);
Chirp_WaitDone();

/* 处理数据 */
for (int i = 0; i < CHIRP_STEPS; i++) {
    int16_t i_val = g_chirp.iq_buf[i].i;
    int16_t q_val = g_chirp.iq_buf[i].q;
    // ...
}
```

### 7.4 帧处理（radar_frame.c）

```c
/* 测量结果结构体 */
typedef struct {
    float water_level_m;        /* 水位（米）*/
    bool  water_level_valid;    /* 水位有效标志 */
    float water_level_snr;      /* 信噪比 */

    float water_velocity_mps;   /* 水速（m/s）*/
    bool  water_velocity_valid; /* 水速有效标志 */

    float car_range_m;          /* 车距（米）*/
    float car_velocity_mps;     /* 车速（m/s）*/
    bool  car_valid;            /* 车辆有效标志 */
} RadarFrame_t;
```

#### API函数

| 函数                     | 功能             | 说明           |
| ------------------------ | ---------------- | -------------- |
| `Radar_MeasureFrame()` | 执行一帧完整测量 | 填充结果结构体 |

#### 内部 static 函数（DSP 算法核心）

**CFAR 恒虚警检测：**

```c
static int cfar_detect(const float *mag, int len);
```

- 输入：幅度谱 `mag`，谱长度 `len`
- 输出：检测到的峰值 bin 索引，-1 表示未检测到目标
- 算法：单元平均 CA-CFAR，2×CFAR_REF=16 个参考单元，2×CFAR_GUARD=4 个保护单元，门限系数 13.0（≈11dB）
- 选信噪比最大的过门限点作为目标

**窗函数初始化：**

```c
static void init_window(void);
```

- 生成 Hanning 窗（旁瓣-31dB），长度 `CHIRP_STEPS=79`，加窗到 128 点 FFT（零填充）
- 内部 `s_window_init` 防重复计算

**Range-FFT 核心（复数输入，含方向修正）：**

```c
static void run_range_fft(const IQ_Sample_t *iq_buf, ChirpDir_t dir, float *mag, float *fft_buf);
```

- 上扫(`CHIRP_UP`)：`Q` 不变，拍频为正，能量在 bin 0~63
- 下扫(`CHIRP_DOWN`)：`Q` 取反（复数共轭），负频率翻转到正频率
- 流程：加窗 → 零填充到 128 点 → `arm_cfft_f32` → `arm_cmplx_mag_f32` 取正频率幅度（64 bins）

**亚 bin 峰值定位（三点抛物线插值）：**

```c
static float refine_peak(const float *mag, int bin, int len);
```

- 偏移量 = `0.5×(y0-y2)/(y0-2y1+y2)`
- 距离误差从 ±0.12m 降到约 ±0.012m

**Bin → 距离换算：**

```c
static float bin_to_range(float bin);
```

- `f_beat = bin × ADC_FS_HZ / FFT_SIZE`
- `d = f_beat × c / (2S)`，当前每 bin ≈ 0.234m

**水位测量（单次上扫 → 水面距离）：**

```c
static bool calc_WaterLevel(const IQ_Sample_t *iq_buf, float *range_m, float *snr);
```

- 固定使用 `CHIRP_UP` → Range-FFT → CFAR → 抛物线插值 → 距离
- SNR 估计：峰值幅度 / bin 1~9 低频段噪声均值

**距离峰复数提取（水速相位差分的前置步骤）：**

```c
static bool ExtractPeakComplex(const IQ_Sample_t *iq_buf, ChirpDir_t dir, 
                               float *real_out, float *imag_out, float *range_m_out);
```

- 返回 CFAR 检测到的目标峰处复数 FFT 系数（实部+虚部）
- 后续调用方通过 `atan2f(im, re)` 提取相位，用于相位差分法测速

**水速计算（相位差分法）：**

```c
static float calc_WaterVelocity(const float *phase_up_seq, int N);
```

- 对 N 个连续上扫相位序列做差分，相位展开（消除 2π 跳变）
- `v = λ × Δφ / (4π × Δt)`，粗过滤 `|v| < 5m/s`
- 最大不模糊速度 ≈ 3.11m/s（λ≈12.44mm，Δt≈2×(Tc+Ts)≈4ms）

**车距车速联立方程求解（三角波法）：**

```c
static bool calc_CarRangeVelocity(const IQ_Sample_t *iq_up, const IQ_Sample_t *iq_dn,
                                  float *range_m, float *velocity_mps);
```

- 上扫拍频 `f_up = (2B/cTc)×R + (2fc/c)×v`
- 下扫拍频 `f_down = (2B/cTc)×R - (2fc/c)×v`
- 联立解：`R = c×Tc×(f_up+f_down)/(4B)`，`v = c×(f_up-f_down)/(4fc)`
- 过滤条件：R∈[0.5, 50]m，|v|<40m/s

#### 测量流程

```
┌──────────────────────────────────────────────────────────┐
│                    Radar_MeasureFrame()                   │
├──────────────────────────────────────────────────────────┤
│  阶段1：水位测量（约2ms）                                  │
│    1次上扫 → Range-FFT → CFAR检测 → 距离                  │
├──────────────────────────────────────────────────────────┤
│  阶段2：水速测量（约34ms）                                 │
│    16对三角波 → 提取相位序列 → 相位差分 → 速度            │
├──────────────────────────────────────────────────────────┤
│  阶段3：车速测量（约10ms，可选）                           │
│    4对三角波 → 联立方程 → 距离+速度                       │
└──────────────────────────────────────────────────────────┘
```

---

## 8. Radar_protocol 通信协议层

### 8.1 模块功能

Radar_protocol 层建立在 SP3485E RS-485 驱动之上，负责将 `RadarFrame_t` 测量结果打包成二进制帧通过 RS-485 发送给上位机。

#### 二进制帧格式

```
帧结构：SOF | TYPE | DEV_ID | SEQ | LEN | PAYLOAD | CRC16
字节数： 2      1      2       2     1     可变       2
```

- SOF：`0x55AA`（帧起始标志）
- TYPE：`0x01`（测量数据帧），`0x81`（ACK 帧）
- DEV_ID：本机设备编号（2 字节，小端）
- SEQ：帧序号（2 字节，小端），每成功发送 +1
- LEN：payload 长度（1 字节）
- PAYLOAD：10 字节固定长度
- CRC16：Modbus CRC16，计算范围 `TYPE~PAYLOAD`，多项式 `0xA001`

**PAYLOAD 布局（10 字节）：**

| 偏移 | 字段           | 类型     | 说明                                             |
| ---- | -------------- | -------- | ------------------------------------------------ |
| 0~1  | status         | uint16_t | 状态位组合（见 status 定义）                     |
| 2~5  | water_level    | int32_t  | 水位，单位 mm（`1000 * water_level_m`）        |
| 6~9  | water_velocity | int32_t  | 水速，单位 mm/s（`1000 * water_velocity_mps`） |

所有多字节字段均为小端格式（低字节在前），float 先定点化再传输，不上传 IEEE754 二进制。

### 8.2 数据结构

```c
/* 状态位定义 */
#define RADAR_STATUS_WATER_LEVEL_VALID    (1U << 0)
#define RADAR_STATUS_WATER_VELOCITY_VALID (1U << 1)
#define RADAR_STATUS_ATTITUDE_VALID       (1U << 2)
#define RADAR_STATUS_PLL_LOCK_OK          (1U << 3)
#define RADAR_STATUS_ADC_OK               (1U << 4)
#define RADAR_STATUS_SIGNAL_WEAK          (1U << 5)
#define RADAR_STATUS_RANGE_OVER_LIMIT     (1U << 6)
#define RADAR_STATUS_VELOCITY_ABNORMAL    (1U << 7)
#define RADAR_STATUS_SELF_TEST_OK         (1U << 8)

typedef enum {
    RADAR_PROTO_OK = 0,
    RADAR_PROTO_ERROR,
    RADAR_PROTO_TIMEOUT,
    RADAR_PROTO_RS485_ERROR,
} RadarProto_Status_t;
```

### 8.3 API 函数

| 函数                                                   | 功能              | 说明                           |
| ------------------------------------------------------ | ----------------- | ------------------------------ |
| `RadarProtocol_Init(dev_id)`                         | 初始化协议层      | 设置设备 ID，SEQ 清零          |
| `RadarProtocol_GetSeq()`                             | 查询当前 SEQ      | 调试用                         |
| `RadarProtocol_SendData(frame, extra_status)`        | 发送二进制测量帧  | 不等 ACK，SEQ 自动递增         |
| `RadarProtocol_SendDataWaitAck(frame, extra_status)` | 可靠发送          | 等 ACK，超时重发，最多 3 次    |
| `RadarProtocol_SendDebug(frame)`                     | 发送 ASCII 调试帧 | 串口助手观察用，不参与 SEQ/ACK |

### 8.4 内部 static 函数

| 函数                                                | 功能                  | 说明                                      |
| --------------------------------------------------- | --------------------- | ----------------------------------------- |
| `put_u16_le(buf, value)`                          | 小端写入 16 位        | `buf[0]=LSB, buf[1]=MSB`                |
| `put_i32_le(buf, value)`                          | 小端写入 32 位        | 支持负数（uint32_t 转换）                 |
| `get_u16_le(buf)`                                 | 小端读取 16 位        | 用于解析 ACK 帧                           |
| `float_to_i32_1000(value)`                        | float → 定点化 int32 | `value * 1000 ± 0.5` 四舍五入          |
| `make_status(frame, extra_status)`                | 合成 status 位        | 测量有效位 + 硬件状态位                   |
| `crc16_modbus(data, len)`                         | CRC16(Modbus) 计算    | 初值 `0xFFFF`，多项式 `0xA001`        |
| `build_frame(type, dev_id, seq, payload, ...)`    | 通用帧封装            | 拼装 SOF~CRC16 完整帧                     |
| `build_data_frame(frame, extra_status, seq, ...)` | 测量数据帧封装        | 从 `RadarFrame_t` 生成二进制 payload    |
| `build_debug_frame(frame, ...)`                   | ASCII 调试帧封装      | 格式：`$RADAR,WL:1.234,WV:0.123*XX\r\n` |
| `calc_xor(str, len)`                              | ASCII 帧 XOR 校验     | `$` 和 `*` 之间的正文 XOR 求和        |
| `rs485_to_proto_status(rs485_status)`             | 驱动层状态转换        | `RS485_OK→RADAR_PROTO_OK` 等           |
| `is_ack_frame(buf, len, dev_id, seq)`             | ACK 帧判断            | 检查 SOF/CRC/TYPE/DEV_ID/SEQ              |
| `wait_ack(dev_id, seq, timeout_ms)`               | 等待 ACK              | 从 RS-485 环形缓冲搜索 ACK                |

### 8.5 通信流程

```
雷达（下位机）                          上位机
     │                                      │
     │── SOF│TYPE=0x01│DEV_ID│SEQ│LEN│PAYLOAD│CRC16 ──►│  测量数据帧
     │                                      │
     │◄── SOF│TYPE=0x81│DEV_ID│SEQ│LEN=0│CRC16 ──│  ACK 帧
     │                                      │
  SEQ++                                     │
     │                                      │
     │  （下一帧）                          │
```

---

## 9. 系统状态变量管理

### 9.1 现状问题

在 `main.c` 中，`RADAR_STATUS_PLL_LOCK_OK`、`RADAR_STATUS_ADC_OK`、`RADAR_STATUS_SELF_TEST_OK` 目前是硬编码常开：

```c
uint16_t extra_status = RADAR_STATUS_PLL_LOCK_OK |
                        RADAR_STATUS_ADC_OK |
                        RADAR_STATUS_SELF_TEST_OK;
```

这不符合实际情况——这些状态位应根据硬件初始化结果和运行时的实时监测动态设置。

### 9.2 修改方案

#### 方案一：全局状态变量（推荐，最小改动）

在 `main.c` 或单独的状态管理头文件中定义全局状态变量：

```c
/* 建议放在 main.c USER CODE BEGIN PV 区域 */
static uint16_t s_hw_status = 0U;

/* 状态更新辅助宏 */
#define HW_STATUS_SET(bit)     (s_hw_status |= (bit))
#define HW_STATUS_CLR(bit)     (s_hw_status &= ~(bit))
#define HW_STATUS_GET(bit)     ((s_hw_status & (bit)) != 0)
```

**初始化阶段逐一检查硬件：**

```c
/* 1. PLL 初始化 + 锁定检查 */
ADF4153A_Init(&pll_cfg);
if (ADF4153A_WaitLock(500)) {
    HW_STATUS_SET(RADAR_STATUS_PLL_LOCK_OK);
    RS485_SendStr("$RADAR,PLL:LOCKED\r\n");
} else {
    HW_STATUS_CLR(RADAR_STATUS_PLL_LOCK_OK);
    RS485_SendStr("$RADAR,ERR:PLL_LOCK_FAIL\r\n");
    Error_Handler();
}

/* 2. BGT24MTR11 初始化 */
BGT24MTR11_Init(&bgt_cfg);

/* 3. ADC 自检（读取温度通道或注入通道验证通信）*/
if (ADC_SelfTest() == HAL_OK) {
    HW_STATUS_SET(RADAR_STATUS_ADC_OK);
}

/* 4. 自检完成 */
HW_STATUS_SET(RADAR_STATUS_SELF_TEST_OK);
```

**主循环中直接使用：**

```c
while (1) {
    Radar_MeasureFrame(&frame);
    RadarProtocol_SendData(&frame, s_hw_status);
    HAL_Delay(50);
}
```

#### 方案二：运行时动态监测（更完善）

在每帧测量中或定时检测硬件状态：

```c
static void UpdateHWStatus(void)
{
    /* PLL 锁定状态：轮询 MUXOUT 引脚（不阻塞，只拍一次快照）*/
    if (HAL_GPIO_ReadPin(ADF_MUXOUT_GPIO_Port, ADF_MUXOUT_Pin) == GPIO_PIN_SET) {
        HW_STATUS_SET(RADAR_STATUS_PLL_LOCK_OK);
    } else {
        HW_STATUS_CLR(RADAR_STATUS_PLL_LOCK_OK);
    }

    /* ADC 状态：检查 DMA 是否正常响应 */
    /* 如果在预期时间内没有 DMA 完成中断，判定 ADC 异常 */
    static uint32_t last_adc_tick = 0;
    if (s_dma_done || (HAL_GetTick() - last_adc_tick < 100)) {
        HW_STATUS_SET(RADAR_STATUS_ADC_OK);
    } else {
        HW_STATUS_CLR(RADAR_STATUS_ADC_OK);
    }
}
```

#### 方案三（推荐）：初始化固定 + 关键位动态监测

在实际项目中，建议采用折中方案：

- **PLL_LOCK_OK**：初始化时锁定后即置位，只有在 ADF4153A 失锁时才清除。失锁检测可以通过在主循环中每隔 N 帧读一次 MUXOUT 引脚实现。
- **ADC_OK**：初始化自检后置位，ADC 连续工作模式下通常不会自行失效，除非 DMA 异常。在 `HAL_ADC_ErrorCallback` 中清除此位。
- **SELF_TEST_OK**：仅初始化时置位，表示系统上电自检通过，运行时不会清除。

综合实现：

```c
static uint16_t s_hw_status = 0U;

/* 初始化阶段 */
void System_HW_Init(void)
{
    /* PLL */
    ADF4153A_Init(&pll_cfg);
    if (ADF4153A_WaitLock(500)) {
        s_hw_status |= RADAR_STATUS_PLL_LOCK_OK;
    }

    /* BGT */
    BGT24MTR11_Init(&bgt_cfg);

    /* ADC 自检 */
    if (ADC_SelfTest() == HAL_OK) {
        s_hw_status |= RADAR_STATUS_ADC_OK;
    }

    /* Chirp 预计算 */
    Chirp_Precompute();

    /* 自检完成 */
    s_hw_status |= RADAR_STATUS_SELF_TEST_OK;
}

/* 运行时周期检查（在主循环中每 100 帧调用一次）*/
void System_HW_Check(void)
{
    /* PLL 运行时失锁检测 */
    if (HAL_GPIO_ReadPin(ADF_MUXOUT_GPIO_Port, ADF_MUXOUT_Pin) == GPIO_PIN_RESET) {
        s_hw_status &= ~RADAR_STATUS_PLL_LOCK_OK;
        /* 可选：触发 PLL 重锁定流程 */
    }
}

/* ADC 错误回调 */
void HAL_ADC_ErrorCallback(ADC_HandleTypeDef *hadc)
{
    if (hadc->Instance == ADC1 || hadc->Instance == ADC2) {
        s_hw_status &= ~RADAR_STATUS_ADC_OK;
    }
}

/* 主循环使用 */
int main(void)
{
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_UART4_Init();

    RS485_Init();
    RadarProtocol_Init(1U);

    System_HW_Init();  /* 统一硬件初始化 */

    RadarFrame_t frame;
    uint32_t frame_count = 0;

    while (1) {
        Radar_MeasureFrame(&frame);

        /* 每 100 帧做一次硬件巡检 */
        if (++frame_count % 100 == 0) {
            System_HW_Check();
        }

        RadarProtocol_SendData(&frame, s_hw_status);
        HAL_Delay(50);
    }
}
```

### 9.3 ADC_SelfTest 示例实现

```c
static HAL_StatusTypeDef ADC_SelfTest(void)
{
    /* 简单自检：启动一次软件触发的 ADC 转换，检查结果是否在合理范围 */
    uint32_t adc_val;
    HAL_ADC_Start(&hadc1);
    if (HAL_ADC_PollForConversion(&hadc1, 10) == HAL_OK) {
        adc_val = HAL_ADC_GetValue(&hadc1);
        HAL_ADC_Stop(&hadc1);
        /* 正常 ADC 输入范围 0~4095，若读到 0 或 4095 异常 */
        if (adc_val > 0 && adc_val < 4095) {
            return HAL_OK;
        }
    }
    return HAL_ERROR;
}
```

---

## 10. 典型应用示例

### 10.1 完整初始化流程

```c
#include "main.h"
#include "ADF4153A.h"
#include "BGT24MTR11.h"
#include "PGA113.h"
#include "SP3485E.h"
#include "ADXL345.h"
#include "radar_chirp.h"
#include "radar_frame.h"

int main(void)
{
    /* HAL初始化 */
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_I2C1_Init();   /* ADXL345需要 */
    MX_UART4_Init();  /* RS-485需要 */
    MX_TIM2_Init();
    MX_ADC1_Init();
    MX_ADC2_Init();

    /* RS-485初始化 */
    RS485_Init();
    RS485_SendStr("$RADAR,BOOT:OK\r\n");

    /* PLL初始化 */
    ADF4153A_Config_t pll_cfg = {
        .INT = 60, .FRAC = 0, .MOD = 125,
        .muxout = MUXOUT_DLOCK_DIGITAL,
        .cp_current = 7, .pd_polarity = 1,
        .noise_spur = NOISE_SPUR_LOWEST_NOISE,
        // ... 其他参数
    };
    ADF4153A_Init(&pll_cfg);
    if (!ADF4153A_WaitLock(500)) {
        RS485_SendStr("$RADAR,ERR:PLL\r\n");
        Error_Handler();
    }

    /* 雷达前端初始化 */
    BGT24MTR11_Config_t bgt_cfg = {
        .tx_disable = 0, .tx_pa_level = 7,
        .dis_div16 = 0, .amux = BGT_AMUX_VTEMP,
        // ... 其他参数
    };
    BGT24MTR11_Init(&bgt_cfg);

    /* PGA初始化 */
    PGA113_Init();
    PGA113_SetGainChannel(PGA113_GAIN_10, PGA113_CH_CH1);

    /* 加速度计初始化 */
    ADXL345_Config_t accel_cfg = {
        .data_rate = ADXL345_RATE_100,
        .range = ADXL345_RANGE_2G,
        .full_res = true,
        .addr_high = false,
    };
    ADXL345_Init(&accel_cfg);

    /* 预计算频率表 */
    Chirp_Precompute();

    /* 主循环 */
    RadarFrame_t frame;
    while (1) {
        Radar_MeasureFrame(&frame);

        /* 上报结果 */
        char buf[80];
        snprintf(buf, sizeof(buf), "$RADAR,WL:%.3f,WV:%.3f\r\n",
                 frame.water_level_m, frame.water_velocity_mps);
        RS485_SendStr(buf);

        HAL_Delay(50);
    }
}
```

### 10.2 ADXL345姿态检测示例

```c
/* 计算倾斜角度 */
ADXL345_AccelData_t accel;
ADXL345_ReadAccel(&accel);

float x_g = ADXL345_RawToG(accel.x, ADXL345_RANGE_2G, true);
float y_g = ADXL345_RawToG(accel.y, ADXL345_RANGE_2G, true);
float z_g = ADXL345_RawToG(accel.z, ADXL345_RANGE_2G, true);

/* 计算俯仰角和横滚角（弧度）*/
float pitch = atan2f(x_g, sqrtf(y_g*y_g + z_g*z_g));
float roll  = atan2f(y_g, sqrtf(x_g*x_g + z_g*z_g));

/* 转换为角度 */
float pitch_deg = pitch * 180.0f / 3.14159f;
float roll_deg  = roll  * 180.0f / 3.14159f;
```

### 10.3 上位机通信协议

**发送格式（雷达→上位机）：**

```
$RADAR,WL:1.234,WV:0.123,CR:5.67,CV:8.3*XX\r\n
```

| 字段 | 含义               | 单位 |
| ---- | ------------------ | ---- |
| WL   | 水位（距水面距离） | m    |
| WV   | 水速               | m/s  |
| CR   | 车距               | m    |
| CV   | 车速               | km/h |
| XX   | XOR校验            | -    |

**接收命令（上位机→雷达）：**

```
$CMD,RATE:1000\r\n    // 设置上报间隔1000ms
$CMD,GAIN:20\r\n      // 设置PGA增益×20
```

---

## 附录：CubeMX配置要点

### GPIO配置

| 引脚 | 模式      | 速度      | 初始状态         |
| ---- | --------- | --------- | ---------------- |
| PB8  | OUTPUT_PP | VERY_HIGH | LOW（SPI数据）   |
| PB9  | OUTPUT_PP | VERY_HIGH | LOW（SPI时钟）   |
| PB11 | OUTPUT_PP | VERY_HIGH | HIGH（PGA CS）   |
| PB12 | OUTPUT_PP | VERY_HIGH | HIGH（BGT CS）   |
| PB13 | OUTPUT_PP | VERY_HIGH | HIGH（ADF LE）   |
| PB0  | INPUT     | -         | -（MUXOUT）      |
| PC4  | OUTPUT_PP | HIGH      | LOW（RS485 DIR） |

### I2C配置（ADXL345）

- I2C1：Standard Mode（100kHz）或 Fast Mode（400kHz）
- PC0：SCL
- PC1：SDA

### UART配置（RS-485）

- UART4：115200bps，8N1
- 开启全局中断

### TIM2配置（Chirp定时）

- Clock Source：Internal Clock
- Prescaler：83（84MHz→1MHz）
- Counter Period：12（13μs周期）
- 开启更新中断

### ADC配置（双通道同步）

- Mode：Dual Regular Simultaneous
- DMA Access Mode：2
- ADC1开启DMA（Peripheral To Memory，Word）
- External Trigger：Software trigger

---

*文档版本：V1.0*
*更新日期：2026-04*
