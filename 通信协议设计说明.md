# 雷达检测仪通信协议设计说明

## 1. 设计目标

本通信协议用于河道毫米波雷达检测仪与上位机之间的数据交互。系统完成水位和水流速度等物理量解算后，需要将测量结果上传至上位机，并进一步写入数据库或用于可视化界面显示。

协议设计目标如下：

1. 支持水位、水流速度和状态字等核心测量数据上传；
2. 支持多设备接入时的设备区分；
3. 支持帧序号，用于判断丢帧、重复帧和 ACK 对应关系；
4. 支持 CRC16 校验，提高数据传输可靠性；
5. 支持上位机 ACK 应答，便于后续实现超时重发；
6. 保留调试帧格式，方便开发阶段通过串口助手直接观察数据；
7. 应用帧采用二进制格式，便于数据库和可视化系统稳定解析。

## 2. 通信链路

系统采用 RS-485 半双工通信方式。下位机为 STM32 雷达检测仪，上位机可以是 PC 测试程序、工控机、网关或服务器采集端。

基本通信方向如下：

```text
STM32雷达检测仪  →  RS-485发送测量数据帧  →  上位机/网关
STM32雷达检测仪  ←  RS-485回复ACK确认帧   ←  上位机/网关
```

下位机发送测量帧后，上位机接收并进行帧头识别、长度判断、CRC 校验和字段解析。若数据帧正确，上位机返回 ACK 帧；若 CRC 错误或帧结构异常，可以丢弃该帧，或者扩展 NACK 错误应答。

## 3. 协议分层

协议可以分为两层：

```text
底层传输层：RS-485 半双工串口
    负责字节传输、收发方向切换、帧边界识别、CRC校验、ACK确认和超时重发

应用数据层：雷达测量数据
    负责设备ID、帧序号、状态字、水位、水流速度等字段定义
```

这样设计的好处是，后期如果底层通信方式由 RS-485 扩展为 4G、LoRa、以太网或 MQTT，应用数据层仍可基本保持不变。

## 4. 调试帧与应用帧

软件通信模块同时保留两类数据帧：

### 4.1 ASCII 调试帧

调试帧主要用于开发阶段。其特点是可读性好，使用普通串口助手即可直接观察水位和水速数据。

示例格式：

```text
$RADAR,WL:1.234,WV:0.123*XX\r\n
```

字段含义：

```text
$        帧起始符
RADAR    帧类型标识
WL       Water Level，水位，单位 m
WV       Water Velocity，水流速度，单位 m/s
XX       XOR校验，两位十六进制
\r\n     帧结束符
```

调试帧适合串口调试，不作为最终数据库上传的主要协议格式。

### 4.2 二进制应用帧

二进制应用帧用于正式数据上传。其特点是字段固定、数据紧凑、校验可靠，适合上位机程序自动解析、入库和可视化展示。

最终推荐帧结构为：

```text
SOF | TYPE | DEV_ID | SEQ | LEN | PAYLOAD | CRC16
```

其中不设置 `VER` 和 `TIME` 字段。原因如下：

1. 当前系统协议短期内不需要多版本并行解析，`TYPE` 和 `LEN` 已具备基本扩展能力；
2. 当前硬件未配置具备断电保持能力的 RTC 或可靠外部时间源，因此不在设备端生成时间戳；
3. 数据库时间建议由上位机或服务器在接收并校验成功后添加，即 `server_receive_time`。

## 5. 二进制帧格式定义

### 5.1 总体帧结构

| 字段    |   长度 | 类型     | 说明                    |
| ------- | -----: | -------- | ----------------------- |
| SOF     | 2 字节 | uint16_t | 帧头，固定为 `0x55AA` |
| TYPE    | 1 字节 | uint8_t  | 帧类型                  |
| DEV_ID  | 2 字节 | uint16_t | 设备 ID                 |
| SEQ     | 2 字节 | uint16_t | 帧序号                  |
| LEN     | 1 字节 | uint8_t  | PAYLOAD 数据区长度      |
| PAYLOAD | N 字节 | bytes    | 数据区                  |
| CRC16   | 2 字节 | uint16_t | CRC16 校验值            |

建议统一采用小端格式：

```text
uint16_t：低字节在前，高字节在后
int32_t ：低字节在前，高字节在后
CRC16   ：低字节在前，高字节在后
```

### 5.2 CRC16 计算范围

CRC16 对以下内容计算：

```text
TYPE | DEV_ID | SEQ | LEN | PAYLOAD
```

不包括：

```text
SOF
CRC16本身
```

建议采用 Modbus CRC16，初值 `0xFFFF`，多项式 `0xA001`。

## 6. 帧类型 TYPE 定义

当前最小实现只需要测量数据帧和 ACK 确认帧，但保留扩展类型。

|     TYPE | 名称          | 含义        | 当前实现建议 |
| -------: | ------------- | ----------- | ------------ |
| `0x01` | RADAR_DATA    | 测量数据帧  | 必须实现     |
| `0x02` | DEVICE_STATUS | 设备状态帧  | 可选         |
| `0x03` | DEBUG_INFO    | 调试信息帧  | 可选         |
| `0x10` | SET_PARAM     | 参数设置帧  | 预留         |
| `0x11` | READ_PARAM    | 参数读取帧  | 预留         |
| `0x81` | ACK           | 确认应答帧  | 建议实现     |
| `0x82` | NACK          | 错误应答帧  | 可选         |
| `0xFF` | ERROR         | 异常/错误帧 | 可选         |

当前阶段建议先实现：

```text
0x01：测量数据帧
0x81：ACK确认帧
```

## 7. 设备 ID：DEV_ID

`DEV_ID` 用于区分不同雷达检测仪。即使当前系统只有单个设备，也建议保留该字段，便于后续多个节点挂接到同一 RS-485 总线或接入同一上位机平台。

建议类型：

```c
uint16_t dev_id;
```

范围为 `0 ~ 65535`，对于本科毕设样机和小规模多设备系统已经足够。

## 8. 帧序号：SEQ

`SEQ` 用于判断丢帧、重复帧和 ACK 对应关系，不作为时间戳使用。

建议生成方式：

1. 设备上电后，`SEQ` 从 0 开始；
2. 每成功发送一帧新的测量数据，`SEQ` 加 1；
3. 达到 `65535` 后自动回绕到 0；
4. 如果设备重启后 `SEQ` 重新从 0 开始，上位机可以认为设备发生重启或通信重新开始。

建议类型：

```c
uint16_t seq;
```

发送和 ACK 对应关系：

```text
下位机发送：TYPE=0x01, SEQ=N
上位机应答：TYPE=0x81, SEQ=N
```

设备端收到 ACK 后，应检查：

```text
TYPE 是否为 0x81
DEV_ID 是否等于本机设备号
SEQ 是否等于当前等待确认的数据帧序号
CRC16 是否正确
```

全部满足时，本帧发送成功。

## 9. 测量数据帧 PAYLOAD 设计

当 `TYPE = 0x01` 时，PAYLOAD 为雷达测量数据区。

### 9.1 最小版 PAYLOAD

建议先实现最小版数据区：

| 字段           | 类型     |   长度 | 单位 | 说明     |
| -------------- | -------- | -----: | ---- | -------- |
| status         | uint16_t | 2 字节 | -    | 状态字   |
| water_level    | int32_t  | 4 字节 | mm   | 水位     |
| water_velocity | int32_t  | 4 字节 | mm/s | 水流速度 |

最小版 PAYLOAD 长度：

```text
2 + 4 + 4 = 10 字节
```

### 9.2 扩展版 PAYLOAD

后续可扩展为：

| 字段           | 类型     |   长度 | 单位   | 说明                   |
| -------------- | -------- | -----: | ------ | ---------------------- |
| status         | uint16_t | 2 字节 | -      | 状态字                 |
| water_level    | int32_t  | 4 字节 | mm     | 水位                   |
| water_velocity | int32_t  | 4 字节 | mm/s   | 水流速度               |
| range          | int32_t  | 4 字节 | mm     | 雷达到水面的距离，可选 |
| angle          | int16_t  | 2 字节 | 0.01° | 安装倾角，可选         |
| snr            | uint16_t | 2 字节 | 0.1 dB | 信噪比，可选           |

其中物理量采用定点整数，而不直接传输浮点数。原因是定点数跨平台解析更稳定，也更便于数据库字段存储。

换算关系：

```text
water_level_m       = water_level / 1000.0
water_velocity_mps  = water_velocity / 1000.0
range_m             = range / 1000.0
angle_deg           = angle / 100.0
snr_db              = snr / 10.0
```

## 10. 状态字 status 设计

`status` 用于说明当前帧中各物理量是否有效，以及设备状态是否正常。上位机和数据库应优先根据 `status` 判断数据是否可信，而不是只看数值字段。

建议按位定义：

|         位 | 名称                 | 含义         |
| ---------: | -------------------- | ------------ |
|       bit0 | WATER_LEVEL_VALID    | 水位有效     |
|       bit1 | WATER_VELOCITY_VALID | 水流速度有效 |
|       bit2 | ATTITUDE_VALID       | 姿态数据有效 |
|       bit3 | PLL_LOCK_OK          | PLL 锁定正常 |
|       bit4 | ADC_OK               | ADC 采样正常 |
|       bit5 | SIGNAL_WEAK          | 雷达信号过弱 |
|       bit6 | RANGE_OVER_LIMIT     | 距离超量程   |
|       bit7 | VELOCITY_ABNORMAL    | 水速异常     |
|       bit8 | SELF_TEST_OK         | 设备自检正常 |
| bit9~bit15 | RESERVED             | 预留         |

示例：

```text
status = 0x0003
```

表示：

```text
bit0 = 1，水位有效
bit1 = 1，水流速度有效
```

如果某帧没有检测到水面峰，可以设置：

```text
bit0 = 0
SIGNAL_WEAK 或 RANGE_OVER_LIMIT 根据实际情况置位
```

## 11. ACK 确认帧设计

当 `TYPE = 0x81` 时，表示 ACK 确认帧。ACK 帧用于上位机确认已经正确接收到某一帧测量数据。

ACK 帧结构：

```text
SOF | TYPE | DEV_ID | SEQ | LEN | CRC16
```

其中：

```text
SOF     = 0x55AA
TYPE    = 0x81
DEV_ID  = 被确认的数据帧设备ID
SEQ     = 被确认的数据帧序号
LEN     = 0
CRC16   = 对 TYPE、DEV_ID、SEQ、LEN 计算
```

ACK 帧没有 PAYLOAD。

## 12. 发送与重发机制

建议的可靠发送流程如下：

```text
物理量解算完成
    ↓
填充测量数据结构
    ↓
物理量定点化
    ↓
填充二进制帧字段
    ↓
计算CRC16
    ↓
通过RS-485发送
    ↓
等待ACK
    ↓
收到匹配ACK？
    ├─ 是：发送成功，SEQ递增，进入下一帧
    └─ 否：超时重发，最多重发N次
```

建议参数：

```text
ACK超时时间：100 ms（测试阶段可适当加长）
最大重发次数：3 次
```

如果超过最大重发次数仍未收到有效 ACK，可以记录通信错误状态，并进入下一测量周期或等待下一次发送机会。

## 13. 上位机接收处理流程

上位机或网关接收二进制帧时，建议流程如下：

```text
串口接收字节流
    ↓
查找帧头 0x55AA
    ↓
读取 TYPE、DEV_ID、SEQ、LEN
    ↓
根据 LEN 判断整帧长度
    ↓
读取 PAYLOAD 和 CRC16
    ↓
计算 CRC16 并比较
    ↓
CRC正确？
    ├─ 否：丢弃该帧或返回NACK
    └─ 是：解析PAYLOAD
             ↓
          转换定点数为物理量
             ↓
          添加上位机/服务器接收时间
             ↓
          写入数据库/更新可视化界面
             ↓
          返回ACK
```

数据库建议字段包括：

```text
device_id
seq
server_receive_time
water_level_m
water_velocity_mps
status
water_level_valid
water_velocity_valid
raw_frame_hex
```

其中 `server_receive_time` 由上位机或服务器生成。

## 14. Radar_protocol.c/.h建议

主要职责：

```text
测量结果定点化
状态字打包
二进制帧封装
CRC16计算
ASCII调试帧封装
ACK帧解析
发送超时与重发逻辑
```

建议模块边界：

```text
radar_frame.c
    只负责雷达物理量解算，输出 RadarFrame_t

radar_protocol.c
    负责将 RadarFrame_t 封装为调试帧或二进制应用帧
    负责CRC、SEQ、DEV_ID、状态字、ACK解析和重发

SP3485E.c
    负责底层RS-485发送、接收、方向切换、中断
```

不要把协议封装和 ACK 处理直接塞入 `radar_frame.c`，否则算法层和通信层会耦合过深。

## 15. 最小测试步骤

### 第一步：固定数据单向发送

下位机先周期性发送固定数据，不等待 ACK：

```text
status = 0x0003
water_level = 1234      // 1.234 m
water_velocity = 123    // 0.123 m/s
```

上位机只负责接收、打印十六进制、校验 CRC、解析字段。

### 第二步：上位机自动回复 ACK

上位机收到并校验 `TYPE=0x01` 的数据帧后，回复：

```text
TYPE = 0x81
DEV_ID = 原数据帧DEV_ID
SEQ = 原数据帧SEQ
LEN = 0
CRC16 = 重新计算
```

### 第三步：下位机增加 ACK 等待和重发

下位机发送数据帧后等待 ACK。如果超时未收到匹配 ACK，则重发当前帧，最多重发 3 次。

## 16. 论文表述建议

论文中可以这样概括：

```text
为兼顾调试便利性和后续工程应用需求，软件通信模块同时设计了 ASCII 调试帧和二进制应用帧。调试帧便于开发阶段通过串口工具直接观察水位、水速等数据；二进制应用帧则面向数据库存储和可视化系统接入，帧内包含设备编号、帧序号、状态字、定点化测量数据和 CRC16 校验。由于系统未配置具备断电保持能力的实时时钟，数据帧不携带设备端时间戳，而由上位机或服务器在接收并校验成功后添加接收时间。为提高传输可靠性，接收端校验正确后返回 ACK 帧，发送端通过帧序号匹配确认当前帧是否成功发送，并在超时情况下进行有限次数重发。
```

## 17. 最终推荐结论

最终协议采用：

```text
SOF | TYPE | DEV_ID | SEQ | LEN | PAYLOAD | CRC16
```

删除：

```text
VER
TIME
```

保留：

```text
TYPE
DEV_ID
SEQ
LEN
status
CRC16
ACK
```

调试阶段保留 ASCII 帧，正式应用采用二进制帧。
